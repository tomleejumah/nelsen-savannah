name: Web CI

on:
  push:
    branches: [main]
    paths:
      - "web/**"
      - ".github/workflows/web.yml"
  pull_request:
    branches: [main]
    paths:
      - "web/**"
      - ".github/workflows/web.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: Install
        run: npm ci
      - name: Build
        run: npm run build
      - name: Upload static site
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: web-dist
          path: web/dist/client
          retention-days: 3

  deploy:
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: self-hosted
    steps:
      - name: Download static site
        uses: actions/download-artifact@v4
        with:
          name: web-dist
          path: web-dist

      - name: Deploy to nginx root
        env:
          # Static SPA served by nginx on :8012 (nelsen-savannah.tommlyjumah.dev).
          # No PM2 — API already owns the `nelsen-savannah` process name.
          WEB_DIR: /home/server/web-projects/nelsen-savannah
        run: |
          mkdir -p "$WEB_DIR"
          rsync -av --delete \
            --exclude '.git' \
            "${GITHUB_WORKSPACE}/web-dist/" "$WEB_DIR/"
          # nginx (www-data) must be able to read through /home/server (711).
          find "$WEB_DIR" -type d -exec chmod 755 {} +
          find "$WEB_DIR" -type f -exec chmod 644 {} +

  smoke:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - name: Live site smoke
        run: |
          set -euo pipefail
          code=$(curl -sS -o /dev/null -w "%{http_code}" \
            https://nelsen-savannah.tommlyjumah.dev/)
          test "$code" = "200"
          # SPA fallback for client routes
          code=$(curl -sS -o /dev/null -w "%{http_code}" \
            https://nelsen-savannah.tommlyjumah.dev/learning)
          test "$code" = "200"
          echo "web smoke ok"
